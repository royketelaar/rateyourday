<div class="row">
	<div class="col12">
		<h2>Beantwoord</h2>
		<% @answer.each do |a| %>
		<div>
			Vraag:<%= Question.find(a.question_id).question %>
			Antwoord:<%= a.answer %>
		</div>
		<% end %>
	</div>
</div>